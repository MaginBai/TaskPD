# Функция с несколькими параметрами со значениями по умолчанию
def calculate_area(length=1, width=1): return length*width


print(calculate_area(4))
print(calculate_area(4, 3))
print(calculate_area())
